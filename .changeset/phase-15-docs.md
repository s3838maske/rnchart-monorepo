---
'@rnchart/charts': minor
'@rnchart/core': minor
'@rnchart/skia': minor
---

Phase 15 — documentation and release preparation.

README rebuilt around real device captures, an honest comparison table against
gifted-charts / victory-native / chart-kit, and an explicit "what's not done"
section. Seven documentation pages under `docs/`. TypeDoc API reference via
`yarn docs:api`. `yarn prepublish:check` verifies every release gate and runs
in CI.

Nothing is published. The publish command stays a human decision.
